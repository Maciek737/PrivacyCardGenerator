// This was added later on in the project
// It is used for creating the exe file
// All it does is call the main.js but its needed for the exe creation :) 
const privacy = require('.')

privacy.data.main();